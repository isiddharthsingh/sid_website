{
  "name": "sid-website",
  "private": true,
  "scripts": {
    "dev": "esbuild --servedir=. --serve=localhost:8000",
    "start": "esbuild --servedir=. --serve=localhost:8000",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0"
  }
}
